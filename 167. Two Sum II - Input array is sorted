class Solution:
    def twoSum(self, numbers: List[int], target: int) -> List[int]:
        low, high = 0, len(numbers) - 1
        while low < high:
            sum = numbers[low] + numbers[high]
            if sum > target:
                high -= 1
            elif sum == target:
                return [low + 1, high + 1]
            else:
                low += 1
        
"""for index questions, always think about two pointers. 
pointers return the index that we wanted. Since the array is already 
sorted. We only need to move left or right to find the one that equals to target.""" 
