class Solution:
    def search(self, nums: List[int], target: int) -> int:
        left, right = 0, len(nums)-1
        while left <= right: 
            pivot = left + (right - left) // 2 
            if nums[pivot] == target: 
                return pivot 
            elif target < nums[pivot]:
                right = pivot - 1
            else: 
                left = pivot + 1
        return -1
            
"""binary search, finding the middle value, if larger than target, right goes to the left. else, left gose to the right. It is more efficient than linear search.

pivot is the middle value 
"""
